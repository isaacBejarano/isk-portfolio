import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'isk-error',
  templateUrl: './p404.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class Page404 {} // DUMMY
